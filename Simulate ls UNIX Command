#include <stdio.h>
#include <dirent.h>

int main()
{
    struct dirent *entry;
    DIR *dp;

    dp = opendir(".");

    printf("Files in current directory:\n");

    while((entry = readdir(dp)) != NULL)
        printf("%s\n", entry->d_name);

    closedir(dp);
    return 0;
}
